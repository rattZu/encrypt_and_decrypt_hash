#include <stdio.h>
#include <conio.h>   // _kbhit() e _getch()
#include <windows.h> // Sleep()

int main() {
    printf("Pressione qualquer tecla para parar...\n");

    while (1) {
        printf("azul\n");
        Sleep(300); // espera 300ms para não lotar a tela

        if (_kbhit()) {   // verifica se alguma tecla foi pressionada
            _getch();     // captura a tecla (opcional)
            break;        // sai do loop
        }
    }

    printf("Programa encerrado!\n");
    return 0;
}